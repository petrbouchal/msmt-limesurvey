---
format: html
editor: source
params:
  blah: this
  id: 1
---

# {{< meta title >}}

```{r setup}
library(dplyr)
library(ggplot2)
library(forcats)
library(stringr)
library(tidyr)
```

```{r}
dta <- readRDS("resps.rds")
```

# `r params$id`

```{r}
print(params$blah)
```

```{r}
odpovedi <- dta |> filter(id == params$id)
```

```{r}
odpovedi |> select(id, starts_with("Q"))
```
